import { Box, Container } from '@mui/material';
import React from 'react';

enum ResearchLevel {
    Incomplete,
    Complete,
    Perfect
};

const ShinyPage: React.FC = () => {

    return (
        <Container>
            <Box>

            </Box>
        </Container>
    );
};

export default ShinyPage;